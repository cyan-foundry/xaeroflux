# Superseded by ci.yml. Intentionally disabled so the repo has a single CI
# definition. Left as a no-op stub because the file could not be deleted in the
# environment that authored it; safe to `git rm` on a normal checkout.
#
# Only workflow_dispatch => GitHub never auto-runs this on push/PR.
name: Rust (disabled — superseded by ci.yml)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by ci.yml; intentionally does nothing."
